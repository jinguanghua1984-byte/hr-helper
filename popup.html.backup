<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>HR Helper</title>
  <style>
    body {
      width: 320px;
      padding: 15px;
      margin: 0;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
      background-color: #f5f5f5;
      overflow-x: hidden;
    }

    h1 {
      margin: 0 0 12px 0;
      font-size: 16px;
      color: #333;
      text-align: center;
    }

    .section {
      background-color: #fff;
      border-radius: 8px;
      padding: 12px;
      margin-bottom: 12px;
    }

    /* 加载职位区域 */
    .load-status {
      margin-bottom: 10px;
      padding: 10px;
      background-color: #f8f9fa;
      border-radius: 6px;
      font-size: 12px;
    }

    .status-line {
      display: flex;
      justify-content: space-between;
      margin-bottom: 5px;
    }

    .status-line:last-child {
      margin-bottom: 0;
    }

    .loading-spinner {
      display: inline-block;
      width: 14px;
      height: 14px;
      border: 2px solid #f3f3f3;
      border-top: 2px solid #4285f4;
      border-radius: 50%;
      animation: spin 1s linear infinite;
      vertical-align: middle;
      margin-right: 5px;
    }

    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }

    .not-available {
      color: #999;
      font-style: italic;
    }

    /* 按钮样式 */
    button {
      width: 100%;
      padding: 10px;
      background-color: #4285f4;
      color: white;
      border: none;
      border-radius: 6px;
      font-size: 13px;
      cursor: pointer;
      transition: background-color 0.2s;
    }

    button:hover:not(:disabled) {
      background-color: #3367d6;
    }

    button:active:not(:disabled) {
      background-color: #245bb6;
    }

    button:disabled {
      background-color: #ccc;
      cursor: not-allowed;
    }

    .collect-btn {
      background-color: #34a853;
    }

    .collect-btn:hover:not(:disabled) {
      background-color: #2d9249;
    }

    /* 数据列表 */
    .candidate-list {
      max-height: 350px;
      overflow-y: auto;
      background-color: #fff;
      border-radius: 6px;
      padding: 8px;
    }

    .candidate-item {
      padding: 10px;
      border-bottom: 1px solid #f0f0f0;
      cursor: pointer;
      transition: background-color 0.2s;
    }

    .candidate-item:last-child {
      border-bottom: none;
    }

    .candidate-item:hover {
      background-color: #f8f9fa;
    }

    .candidate-name {
      font-weight: bold;
      font-size: 13px;
      color: #333;
      margin-bottom: 6px;
    }

    .candidate-info {
      font-size: 11px;
      color: #666;
      display: flex;
      flex-direction: column;
      gap: 3px;
    }

    .candidate-info span {
      display: flex;
      justify-content: space-between;
    }

    .candidate-info span:last-child {
      font-weight: 500;
    }

    /* 详情面板 */
    .detail-panel {
      position: fixed;
      top: 0;
      right: -100%;
      width: 100%;
      max-width: 350px;
      height: 100%;
      background-color: white;
      box-shadow: -2px 0 8px rgba(0,0,0,0.1);
      z-index: 1000;
      transition: right 0.3s ease;
      overflow-y: auto;
    }

    .detail-panel.show {
      right: 0;
    }

    .detail-header {
      position: sticky;
      top: 0;
      background-color: white;
      padding: 12px;
      border-bottom: 1px solid #f0f0f0;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .detail-title {
      font-size: 14px;
      font-weight: bold;
    }

    .detail-close {
      width: 24px;
      height: 24px;
      border: none;
      background: none;
      cursor: pointer;
      padding: 0;
      font-size: 20px;
    }

    .detail-content {
      padding: 12px;
    }

    .detail-field {
      display: flex;
      margin-bottom: 6px;
      font-size: 12px;
    }

    .detail-field-label {
      width: 90px;
      color: #666;
      flex-shrink: 0;
    }

    .detail-field-value {
      flex: 1;
      color: #333;
      word-break: break-all;
    }

    .timeline-item {
      padding: 8px;
      border-left: 2px solid #e0e0e0;
      margin-left: 8px;
      margin-bottom: 8px;
    }

    .timeline-item:last-child {
      margin-bottom: 0;
    }

    .timeline-time {
      font-size: 11px;
      color: #666;
      margin-bottom: 4px;
    }

    .timeline-content {
      font-size: 12px;
      color: #333;
    }

    /* 分页 */
    .pagination {
      margin-top: 12px;
      display: flex;
      justify-content: center;
      gap: 10px;
    }

    .pagination button {
      padding: 6px 14px;
      font-size: 13px;
      background-color: #4285f4;
      color: white;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      transition: background-color 0.2s;
      width: auto;
    }

    .pagination button:hover:not(:disabled) {
      background-color: #3367d6;
    }

    .pagination button:disabled {
      background-color: #ccc;
      cursor: not-allowed;
    }

    .page-info {
      font-size: 11px;
      color: #666;
      text-align: center;
      margin-bottom: 8px;
    }

    .empty {
      text-align: center;
      padding: 20px;
      color: #999;
      font-size: 13px;
    }
  </style>
</head>
<body>
  <h1>📊 HR Helper</h1>

  <!-- 职位数据加载区域 -->
  <div class="section">
    <div class="load-status">
      <div class="status-line">
        <span>状态:</span>
        <span id="loadStatus" class="not-available">不在zhipin.com</span>
      </div>
      <div class="status-line">
        <span>已加载:</span>
        <span id="loadCount">0</span>
      </div>
    </div>
    <button id="loadAllBtn" disabled>加载全部职位</button>
  </div>

  <!-- 人选数据区域 -->
  <div class="section">
    <!-- 采集按钮 -->
    <button id="loadCandidatesBtn" class="collect-btn">采集人选数据</button>

    <!-- 数据列表 -->
    <div class="candidate-list" id="candidateList">
      <div class="empty">点击"采集人选数据"开始采集</div>
    </div>

    <!-- 分页 -->
    <div class="pagination">
      <button id="prevBtn" disabled>上一页</button>
      <button id="nextBtn" disabled>下一页</button>
    </div>
    <div class="page-info" id="pageInfo">第 0 页 / 共 0 页</div>
  </div>

  <!-- 详情面板 -->
  <div class="detail-panel" id="detailPanel">
    <div class="detail-header">
      <div class="detail-title" id="detailTitle"></div>
      <button class="detail-close" id="detailCloseBtn">×</button>
    </div>
    <div class="detail-content" id="detailContent"></div>
  </div>

  <script src="popup.js"></script>
</body>
</html>
